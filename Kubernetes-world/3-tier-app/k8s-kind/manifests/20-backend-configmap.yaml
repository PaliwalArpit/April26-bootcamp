apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: quiz-app
  annotations:
    argocd.argoproj.io/sync-wave: "0"
data:
  APP_VERSION: "1.0.0"
  DB_HOST: quiz-pg-rw.quiz-app.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: devops_learning
  ALLOWED_ORIGINS: "http://localhost,http://frontend-service.quiz-app.svc.cluster.local"
